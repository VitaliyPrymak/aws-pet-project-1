{
  "name": "frontend-app",
  "version": "1.0.0",
  "description": "frontend service",
  "scripts": {
    "start": "serve -s build",
    "build": "react-scripts build"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "serve": "^13.0.0"
  }
}
