name: deploy backend_redis to ECR

on:
  push:
    branches:
      - main
    paths:
      - "backend_redis/**"

jobs:
  deploy-redis-to-ECR:
    runs-on: ubuntu-latest
    steps:
      # Step 1: Checkout Code
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Configure AWS Credentials
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_DEFAULT_REGION }}

      # Step 3: Install AWS CLI
      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      # Step 4: Login to Amazon ECR
      - name: Login to Amazon ECR
        id: login-ecr
        run: |
          aws ecr get-login-password --region ${{ secrets.AWS_DEFAULT_REGION }} | \
          docker login --username AWS --password-stdin ${{ secrets.ECR_REDIS }}

      # Step 5: Build and Push Docker Image
      - name: Build and Push Docker Image
        run: |
          # Generate a short commit hash
          SHORT_SHA=$(echo $GITHUB_SHA | cut -c1-7)
          echo "Building Docker image with SHORT_SHA: $SHORT_SHA"

          # Build Docker image
          docker build -t backend_redis:$SHORT_SHA -f backend_redis/Dockerfile backend_redis

          # Tag the image for ECR
          docker tag backend_redis:$SHORT_SHA ${{ secrets.ECR_REDIS }}:$SHORT_SHA

          # Push the image to ECR
          docker push ${{ secrets.ECR_REDIS }}:$SHORT_SHA

      # Step 6: Install SSH Client
      - name: Install SSH Client
        run: sudo apt-get update && sudo apt-get install -y openssh-client

      # Step 7: Configure SSH Key
      - name: Configure SSH Key
        env:
          PRIVATE_KEY: ${{ secrets.EC2_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$PRIVATE_KEY" | tr -d '\r' > ~/.ssh/ec2_private_key.pem
          chmod 600 ~/.ssh/ec2_private_key.pem

      # Step 8: Add Host Key to Known Hosts
      - name: Add Host Key to Known Hosts
        run: ssh-keyscan -H ${{ secrets.ELASTIC_IP }} >> ~/.ssh/known_hosts

      # Step 9: Update Docker Compose and Deploy
      - name: Update Docker Compose and Deploy
        run: |
          # Define the short commit hash
          SHORT_SHA=$(echo $GITHUB_SHA | cut -c1-7)
          echo "SHORT_SHA: $SHORT_SHA"

          # Log the image reference
          echo "Pulling Image: ${{ secrets.ECR_REDIS }}:$SHORT_SHA"

          # Update docker-compose.yml using yq
          yq eval ".services.backend_redis.image = \"${{ secrets.ECR_REDIS }}:$SHORT_SHA\"" -i docker-compose.yml

          # Copy the updated docker-compose.yml to the remote server
          scp -o StrictHostKeyChecking=no -i ~/.ssh/ec2_private_key.pem docker-compose.yml ec2-user@${{ secrets.ELASTIC_IP }}:/home/ec2-user/docker-compose.yml

          # Connect to the EC2 instance and deploy the Docker image
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/ec2_private_key.pem ec2-user@${{ secrets.ELASTIC_IP }} << 'EOF'
            set -e
            echo "Starting deployment on EC2..."

            export SHORT_SHA=$SHORT_SHA

            # Authenticate with AWS ECR
            aws ecr get-login-password --region ${{ secrets.AWS_DEFAULT_REGION }} | \
              docker login --username AWS --password-stdin 084375565192.dkr.ecr.${{ secrets.AWS_DEFAULT_REGION }}.amazonaws.com

            # Pull the updated image from ECR
            echo "Pulling image: ${{ secrets.ECR_REDIS }}:$SHORT_SHA"
            docker pull ${{ secrets.ECR_REDIS }}:$SHORT_SHA

            # Deploy the updated image with Docker Compose
            docker-compose up -d backend_redis

            echo "Deployment completed successfully!"
          EOF
